{
  "name": "startuphelper-backend",
  "version": "1.0.0",
  "description": "Backend for StartupDash — Yandex Cloud Functions + PostgreSQL",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "deploy": "zip -r function.zip . --exclude '*.git*' --exclude 'node_modules/.cache/*' && echo 'Upload function.zip to Yandex Cloud Functions'"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3",
    "serverless-http": "^3.2.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
